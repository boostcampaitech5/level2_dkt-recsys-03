# sweep/default.yaml example
project: DKT_Sequential
entity: recsys01
program: main.py
method: grid
metric:
  goal: maximize
  name: tr_auc/tr_acc
parameters:
  model:
    values: [LSTM]
  trainer.epoch:
    values: [10, 20, 30]

command:
- ${env}
- ${interpreter}
- ${program}
- ${args_no_hyphens}